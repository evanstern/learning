# Optional: enables LLM-backed planner + quarantine extractor.
# Without it the demo runs fully offline with deterministic fallbacks.
ANTHROPIC_API_KEY=

# Optional: artificial gateway latency in milliseconds (default 0).
GATEWAY_LATENCY_MS=0
